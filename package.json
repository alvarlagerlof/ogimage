{
  "name": "root",
  "main": "index.js",
  "packageManager": "yarn@3.2.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "turbo": "^1.3.4"
  },
  "dependencies": {
    "chalk": "^5.0.1",
    "log-symbols": "^5.1.0",
    "metascraper": "^5.29.18"
  }
}
