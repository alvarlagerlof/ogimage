{
  "name": "root",
  "packageManager": "yarn@3.2.2",
  "scripts": {
    "build": "turbo run build",
    "ogimage": "turbo run ogimage",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "deploy": "turbo run deploy"
  },
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "turbo": "1.4.0"
  }
}
